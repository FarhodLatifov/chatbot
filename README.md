# 🏷️ Генератор хэштегов — Telegram Bot

## Что делает бот

Бот генерирует **все комбинации хэштегов** из списка корней и суффиксов для продвижения в социальных сетях.

### Пример работы

**Вход:**
```
Корни: отопление, котел, котельная
Суффиксы: москва, купить, цена
```

**Выход:**
```
#отоплениемосква #отоплениекупить #отоплениецена
#котелмосква #котелкупить #котелцена
#котельнаямосква #котельнаякупить #котельнаяцена
```

---

## Как работает

1. **Получает** от пользователя список корней и суффиксов
2. **Создаёт** все возможные комбинации `#корень+суффикс`
3. **Удаляет** дубликаты
4. **Разбивает** на блоки по 30 штук (лимит Instagram на комментарий)
5. **Отправляет** готовые блоки для копипаста
6. **Дополнительно** отправляет TXT-файл со всеми хэштегами

---

## Формат ввода

Отправьте боту сообщение:
```
Корни: слово1, слово2, слово3
Суффиксы: окончание1, окончание2
```

> 💡 Можно писать в одну или несколько строк — бот поймёт оба варианта.

---

## Формула расчёта

**Количество хэштегов** = Корни × Суффиксы

| Корни | Суффиксы | Результат |
|-------|----------|-----------|
| 5 | 6 | 30 хэштегов |
| 10 | 10 | 100 хэштегов |
| 20 | 15 | 300 хэштегов |

---

## Установка и запуск

### 1. Установите зависимости
```bash
pip install -r requirements.txt
```

### 2. Настройте токен бота
Создайте файл `.env`:
```
BOT_TOKEN=ваш_токен_от_BotFather
```

### 3. Запустите бота
```bash
python bot.py
```

---

## Получение токена

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен в `.env`

---

## Структура проекта

```
├── bot.py           # Telegram-бот (aiogram)
├── generator.py     # Логика генерации хэштегов
├── config.py        # Настройки (токен, размер блока)
├── requirements.txt # Зависимости
└── .env             # Токен бота (не в Git!)
```

---

## Требования

- Python 3.10+
- aiogram 3.x
- python-dotenv
